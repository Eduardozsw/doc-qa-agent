"""Benchmark de produto do doc-qa-agent (ablação, latência, custo, recuperação).

Executável com `uv run python -m benchmark.run`. Ver `benchmark/run.py` para o
CLI e `docs/benchmark.md` para o relatório mais recente.
"""
